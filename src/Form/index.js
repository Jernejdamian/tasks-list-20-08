import "./style.css";

const Form = () => (
    <form className="form js-form">
        <input className="js-newTasks" /><button className="section__buttons">Dodaj zadanie</button>
    </form>
);

export default Form;